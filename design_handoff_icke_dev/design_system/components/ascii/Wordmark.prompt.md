The logo. Always the literal ASCII art from `assets/wordmark.txt` — never redrawn, never replaced with a font or an SVG.

```jsx
<h1><Wordmark /></h1>
```

Wrap it in the page's `h1` (the `aria-label` carries the accessible name). Scale by font-size only; keep it on the cyan with glow on dark backgrounds.
