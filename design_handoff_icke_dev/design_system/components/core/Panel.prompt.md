Panel is the only card in the system — use it for project rows, dialogs and any grouped content.

```jsx
<Panel active={open}>…</Panel>
```

No radius, no drop shadow; depth comes from the translucent fill over the ASCII field.
